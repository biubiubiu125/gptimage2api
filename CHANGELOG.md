# Changelog

## Unreleased

## 1.0.5 - 2026-09-22

+ [修复] 日志列表/详情契约补齐管理端 `error` 和详情 `raw_detail`，有记录时不再因 `extra=forbid` 整页 500。
+ [修复] 控制台出图改走同源相对 `/images/{path}` 与 `/image-thumbnails/{path}`；worker 只用提交时的 `payload.base_url`，空值不再回填进程 `GPTIMAGE2API_BASE_URL`；`/v1` url 模式仍按请求 `base_url` 拼对外绝对地址。
+ [修复] 控制台任务契约收齐 `task_id`/`client_task_id`/`limit`/`offset`，HTTP 响应与 `ImageTaskRow`/`ImageTaskPage` 共用同一套 `extra=forbid`。
+ [修复] 画廊本地图不再把相对 `/images/` 拼到 `VITE_API_URL`；日志同源改写收本机/内网和已配置的公开 `BASE_URL`，第三方 CDN 即使 path 碰巧是 `/images/` 也不改写。
+ [修复] 日志落库优先用产物 `relative_path` 写成同源 `/images/{path}`，不再把对外绝对地址写进管理端预览。
+ [修复] 控制台 `/api/image-tasks/generations|edits` 日志按文生图/图生图分类，不再落成 other 或对话生图。
+ [修复] `/v1` url-only 结果也校验公开 path 与产物路径一致。
+ [修复] 控制台画廊列表和 `/api/image-tasks` 创建不再强要对外 `GPTIMAGE2API_BASE_URL`；没配时走同源相对 `/images/`，`/v1` 公开 url 模式仍必须配置。
+ [修复] `/v1` url 模式不再把容器内网 stored URL 回落给 NewAPI；没有请求 `base_url` 时只接受公网 stored URL。
+ [修复] 日志预览同源改写与后端对齐：本机/内网和当前 `apiBaseUrl` 同源的 `/images/` 改成相对路径；详情 `raw_detail` 类型收齐 object/array/string。
+ [修复] 控制台任务前端契约补齐 `task_id`/`client_task_id`/`limit`/`offset`，与后端 `ImageTaskRow`/`ImageTaskPage` 对齐。

## 1.0.4 - 2026-09-21

+ [修复] 登录管理端后不再因磁盘上残留的成功更新任务整页死循环刷新。成功态只展示一次就清掉，前端只为当前浏览器自己发起且尚未刷新过的任务自动 reload；`index.html` 不再被缓存。

## 1.0.3 - 2026-09-21

+ [修复] Microsoft 无密码登录的 `authorize/continue` 和 `passwordless/send-otp` 遇真挑战页先刷新 Cookie，再记 Cloudflare 拦截，不再被 JSON-only HTTP 前缀收成授权失败。
+ [修复] 收口验活 403 带 `cf-ray` 头不再抢成 Cloudflare；真挑战页（含无 title 的 JavaScript 拦截页）优先于 HTTP 前缀。
+ [修复] 微软无密码登录换 Token 失败阶段固定「Token 换取」，不再被状态机 `code_wait` 盖成「验证码等待」；live 路径在真正换 Token 前切到 `token_exchange`。
+ [修复] 授权/继续授权 HTTP 失败和 Token 换取失败不再被 debug 里的 `cf-ray` 头抢成 Cloudflare 拦截；真挑战页（Just a moment / `cf-chl-`）仍记成 Cloudflare。
+ [修复] 对外 Authorization 脱敏保留 `Bearer [redacted]`，不再二次替换成 `[redacted] [redacted]`；额度 0 的默认阶段统一「收口验活」；管理端 `/api/proxy/runtime` 和系统设置保留 `cf_clearance` 明文，系统设置可填 Chrome146 手动 Cookie。
+ [修复] 缺回调原文即使带 `/oauth/` 仍记成账号创建失败；继续授权 HTTP 失败阶段固定「继续授权」，不再被 `code_wait` 盖成「验证码等待」。
+ [修复] 微软邮箱不支持无密码登录记成授权失败「继续授权」；暂存 `register_stage` 和失败 payload 的 `failed_from_stage` 也改中文，不再露出 `finalize` / `account_create`。
+ [修复] 注册失败原因不再塞进带 URL / OAuth / `continue=` 的混合原文；短串 `/backend-api/me 403` 记成验活被拦；缺回调按账号创建失败；成功任务阶段也改中文。
+ [修复] 额度 0 和暂存清理失败改黄字「注册警告」四段；空 token 阶段用「Token 换取」；机器阶段 `account_create` 统一展示「创建账号资料」。
+ [修复] 授权/创建账号 HTTP 403 记成授权失败，不再糊成验活被拦；失败四段阶段用中文，不再露出英文 `failed`；发送验证码/提交注册/继续授权的 HTTP 失败也走授权失败。拿到 token 后暂存失败或邮箱回写失败都黄字警告并继续入库；入库失败和号池已满不再被改写成收口失败。管理端主日志、监控原文和数据库 URL 不再抹 token/密码，也不再截断诊断。普通聊天重试不再写 `history_and_training_disabled=False`。
+ [修复] 公开生图失败改为四段中文，管理端保留完整明文分栏。`/v1` 结果不可用原文不再带产物路径或 checksum；读产物失败、失败快照和交付失败四段同样不带内部诊断；resume-poll 缺任务走 404；任务列表单条产物失败不再拖垮整页，该项对外按失败投影。

## 1.0.2 - 2026-09-18

+ [修复] 每次 `main` 发布升版本并自动生成 GitHub Release 更新包，控制台在线更新可用；Docker 镜像 `VERSION` 变化后会重灌运行目录。没升版本或更新包没发出去时不再推 GHCR `latest`；缺资产的 Release 会删掉重发；镜像同时打上 `VERSION` 标签。查询 Release 时只有 404 才算未发布，GitHub 限流或网络错误会挡住发布；latest Release 缺少更新包时控制台不显示可在线更新。
+ [修复] 接码 Session 请求对齐 `curl_cffi` 0.15，DNS 钉死改写到 `session.curl_options`。
+ [修复] `/v1/models` 502、图片任务 404 和产物不可用 409 改为固定公开文案，不再把目录异常、sticky-session 运维提示或产物校验细节返回给客户端。
+ [修复] 图片队列启动先拉心跳再拉调度；调度循环不再抢走还没 `start()` 的心跳线程二次启动，避免 Python 3.13 报 `thread already started` 后容器 502 退出。
+ [修复] 占用门只认 `OCCUPANCY_*`，不再读取未使用的 CPU/内存 pause 环境变量。监控不再显示「注册探测关门」。cgroup v2 `cpu.max=max` 时自动核数不再误读残留 v1 quota；生图上限和 worker 线程地板共用同一套核数。心跳线程已停但调度仍活时，调度循环会把心跳拉起来继续续租；残留心跳还有在手任务、心跳却已停时，健康检查、监控和入队也会把心跳拉起来续租，但仍拒绝新入队。健康检查、监控和入队同时拉心跳时只起一条线程。残留心跳在手任务画完后自己停心跳，没有在手任务后下一次入队再把队列拉起来。健康检查在心跳缺失时仍按不可用；心跳停、调度仍活时占用门保持现场，HTTP 入队不因此拒绝。
+ [修复] 残留心跳未排空时健康检查不再报 ok，占用门按调度未就绪关门；心跳停掉且没有在手任务后，下一次入队会把队列拉起来，身份冲突和备份恢复期间仍拒绝。
+ [修复] 排空成功会看残留心跳：join 超时不再标停机，也不健康队列重置按心跳间隔等待，避免残留心跳把已停工人重新标成活的。
+ [修复] 注册关机超时返回失败，备份恢复不再当成成功去清库。作业租约过期后，只要还在最长领取时间内心跳，仍算活工人，固定 `worker_id` 不能立刻接管。占用门闩来源不再写成注册探测，生图或入队探测会改成对应来源。不健康队列重置时 `stop()` 抛错不当成已排空；残留心跳 join 超时拒绝再开新心跳。
+ [修复] 身份冲突后心跳过期，只要 PostgreSQL 里还有未过期的在画租约，新进程就不能接管同一个 `worker_id`；活工人判定同样看这些租约。领取阶段的身份冲突也会置 fatal。恢复中止不再把身份冲突的调度拉起来。占用放回和失败任务都落空时继续心跳续租。监控查账号空位失败保留上次快照，「当前在画」读本机实际领取。
+ [修复] 备份恢复成功后清掉同进程停机闩，下一次恢复还会再停队列。身份冲突不再立刻停心跳：还在画的任务继续续租，画完或空闲后再停调度。占用放回失败会 `fail_job`，不再只打日志就丢租约。`start()` 在库已关掉或调度被打死且没有在手任务时会重新进恢复，不再因为 `_started` 直接返回。
+ [修复] 队列 dump 写入后立刻落下恢复回收标记，后续普通启动也会收掉快照里未过期的在画租约，不再等成功 after-hook。调度被身份冲突打死或调度线程已停时，HTTP 入队直接拒绝。活工人口径统一按心跳，不再把 `worker_active=False` 当死号；本机产物、终态清理、身份冲突和监控 workers 列表同一套 180 秒窗口。`start()` 不再把 `recover(reclaim_restored=...)` 退化成无参数回收。备份恢复先停注册并立刻打停机事件，恢复期间不再继续自动注册。
+ [修复] 未排空停机即使把 `worker_active=False` 写进快照，心跳仍新时其它副本也不会抢走租约。应用库 `pg_restore --clean` 加 `--single-transaction`，失败整段回滚。领取前占用复查不再把入队时的 T0 快照写回占用门闩。恢复成功后队列 `start(reclaim_restored_leases=True)` 不再退化成无参数 `start()`；队列没起来就不拉注册、GenBox 和备份调度。监控暂停原因在只有未排空副本时仍能读到；恢复标过期工作进程时清空 `process_instance_id`，避免固定 `WORKER_ID` 身份冲突打死调度。
+ [修复] 备份恢复失败不再把调度拉起来对着半截库开工；成功恢复才会重启图片队列，并同时拉回注册、GenBox 和备份调度。应用库 `pg_restore --clean` 前先关掉账号连接池，恢复期间 `/health` 不再查库。恢复后会收掉快照里未过期的在画租约并标过期工作进程，避免幽灵槽占号或固定 `WORKER_ID` 打死调度。领取后占用复查改采现场，不再喂入队时的 T0 快照。监控积压查询失败按关门。
+ [修复] 备份恢复结束后会把图片队列和后台线程拉起来；HTTP 入队在队列未启动时拒绝。含队列的备份先恢复队列 dump，再覆盖应用库。HTTP 入队不再跟 85% 连接池硬墙，新生图仍看连接池占用。监控占用门会推进 2.5 秒计时，积压满时按 `resource_backlog` 关门。线程 join 超时会清掉停机事件并拉回已停服务。恢复范围里的下载/`pg_restore` 超时不再包成「恢复维护准备失败」。派发等完账号快照会再采一次占用门。
+ [修复] 本机生图名额按领取时阶段计算，下载/变换/保存不再把生图槽腾给别的空闲号。备份恢复先停图片队列；未排空立刻中止并拉回已停服务，其它副本只要心跳仍新就拒绝恢复，守卫在任何 `pg_restore` 之前。真正停掉的副本会把心跳标过期，停机不再提前关掉队列连接，恢复才能看其它副本。自动连接池每副本最多 80+40，两副本加应用池仍低于捆绑 PostgreSQL `max_connections=500`。占用关门时派发不再等待账号快照刷新；监控标出入队探测关门，并把调度线程当成未排空。
+ [修复] 保存/变换阶段不再占用本机账号槽，已放租约的空闲号可以立刻再派。停机和备份把还在领取事务里的任务也当成未排空；备份恢复期间除健康检查外 HTTP 返回 503，其它副本心跳仍活着也会拒绝恢复。领取扫描不再长时间锁住 `ImageWorkerState`，跳过原号等待或账号容量时先提交再继续。
+ [修复] 超时停机不再立刻 `release_claim` 放掉还在画的账号租约；未完成领取继续占槽并续租。备份恢复遇到未完成领取会拒绝，恢复队列库前关掉连接。Compose 应用容器 `stop_grace_period` 为 300 秒。图片 SSE 超时上限 1800 秒；SSE 中止会等待内部 stream_task。入队落盘后构造请求失败也会丢弃刚写入的输入。
+ [修复] 派号后账号快照刷新发现不可用时，按 `no_available_account` 有限重试，不再立刻 `fail_job`；仍不换原号。领取后刷新 AT 遇到 OAuth 非 200（含 429/502）按上游失败分类有限重试，不再当成 `internal_error` 立刻失败；作废 refresh token 仍按 `auth_invalid` 换号。OAuth 刷新失败后的 5 分钟退避不再把已过期 AT 交给生图；队列重试会再打 refresh token。OAuth 响应体 `error=internal_error` 仍按 HTTP 状态当上游失败重试，不再立刻 `fail_job`。领取可排队任务时清掉该任务上未过期的幽灵租约，避免 `job_id` 唯一约束把领取打成空转。cgroup CPU 回绕和短间隔下跌保持高压，不把占用门打回空闲。内存硬墙改为剩余不足限额 5% 才拦新入队/新生图，进行中的恢复/下载/保存不再被内存硬墙停掉。自动生图上限按 `max(300, 核数×300)` 计算，不按可用内存收口，也不再封顶 2000。句柄硬墙按进程 `nofile` 留 1024 余量封顶，避免高核自动生图标定超过容器 ulimit 后直接 `EMFILE`。Compose 应用容器 `nofile` 提到 262144；`/v1/ppt`、`/v1/psd` 占用 503 走公开文案。新安装写入占用门 knobs。本地领号失败、`internal_error`、领取超时和额度不足不再按上游错误率关整机生图。未闩占用门时短暂掉到开门线以下不会清掉 2.5 秒高压计时，要持续低占用才重置。cgroup CPU 同一时刻不再改基准样本。监控剩余账号槽现场重算；查槽失败按 0 收口。监控占用门会推进 2.5 秒计时。进程内超时领取继续占槽并续租，等执行线程结束再失败放槽。领取列号等待账号快照刷新。OAuth 退避期间过期 AT 不进候选池。流式等待若已在事件循环上不再回退成同步死等。流式等待用 AnyIO 门户线程等完成，不再占满门口令牌。去掉未使用的生图内存估算常量。自动连接池每副本最多 80+40，捆绑 PostgreSQL `max_connections=500`。
+ [优化] 多机资源自适应：新安装默认 `GPTIMAGE2API_THREAD_TOKENS=0`、`GPTIMAGE2API_IMAGE_QUEUE_GENERATION_CONCURRENCY=0`。生图上限按 `max(300, 核数×300)`，门口令牌与生图上限相同；手填正整数仍覆盖自动。环境变量硬顶默认 99999。CPU/内存/Swap 占用 ≥90% 持续 2.5 秒后停止新入队、新生图和新注册，低于 80% 持续 2.5 秒后恢复。无 Swap 分区忽略 Swap。
+ [修复] 自动线程墙给门口令牌和进程线程留余量，空闲时能开到生图上限。Compose 应用容器 `nofile` 提到 262144、`nproc` 65535，捆绑 PostgreSQL `max_connections=500`。队列连接池占用按 `pool_size + max_overflow` 计算。occupancy 关门时监控有效并发显示 0，账号空位扣除恢复租约和数据库租约。派号失败次数计入超时等未验号失败；卡住 60 秒从本次生图开始算，进入解析不清零。CPU 采样启动时先预热。占用门闩在 80–90% 回差带内不清 2.5 秒计时；worker 未启动时 HTTP 入队仍走占用门；监控读现场门闩，不落后一个派发周期。占用探测和判定拆成两把锁，探测不堵住判定；CPU 占用短间隔抖动不重置 2.5 秒计时。worker 未启动时占用门用当前队列配置。队列单账号并发读后台当前值。新安装积压上限与代码默认对齐为 50。账号空位只计当前可用账号上的租约；worker 未启动时监控仍显示占用门闩；探测或占用门不可用时入队和注册关门。注册只走占用门闩，不跟图片上游失败率一起停。CPU/内存/连接池探测失败按饱和关门，不再当成空闲。0.2 秒内复用磁盘/句柄/连接池探测，CPU/内存/Swap 仍按现场刷新，占用跳升不被探测缓存冻住。worker 未启动时监控会现场采样占用门，有效并发按账号空位显示 0。占用刚闩上时不再继续领取新生图；放回队列时退回这次领取计入的尝试次数，不占用默认 3 次生图预算。from_runtime 只吞队列库 URL 配置错误。Swap/线程/句柄探测失败按饱和关门；句柄计数接口失败也按饱和关门。worker 启动时沿用 HTTP 占用门闩，不再另开一把空控制器。领取前后都会现场重判占用门，慢速列号或入库不会让 2.5 秒门闩失效。恢复池已满把任务放回时同样退回这次领取次数。HTTP 入队走线程/句柄硬墙，不跟连接池和上游错误率硬墙；上游错误率 60 秒无新样本后衰减。队列库已配置却没挂上时连接池按饱和关门。派发列号不再等待账号快照刷新。共用占用门返回空或抛错时注册关门，不再另开一把闩。写盘后再检一次入队容量，硬墙拦住时丢弃刚写入的输入。线程/句柄/连接池/上游硬墙时监控资源门闩显示已关门，有效并发显示 0。worker 在场时监控硬墙现场采样；cgroup CPU 短间隔也刷新；worker 拿不到占用门时按饱和关门；注册导入失败不再另开占用门；同步派号每张在途图单独记开始时间。占用门把同一阶段任务放回后再领取时保留本次生图开始时间，60 秒卡住计时不清零。探测缓存窗口内 CPU 下跌不覆盖已采到的高压，短间隔空闲窗口不重置 2.5 秒计时。账号空位为 0 时不再扫新生图队列。Compose / `.env.example` / README 写出占用门和线程硬墙默认值。
+ [修复] 占用门 CPU 用本进程自己的 `cpu_times` 窗口，不再跟别人调用的 `cpu_percent(interval=None)` 抢短间隔。Linux `guest`/`guest_nice` 已计入 `user`/`nice`，占用统计不再重复加一遍。探测缓存窗口内内存/Swap 下跌也不覆盖已采到的高压。账号空位查不到租约时按满额关门；过期闲置租约仍忽略，进行中任务的过期槽继续占位。心跳按租约令牌续期，已过期的在手任务也能救回来；checkpoint/完成/失败/重试/放回同样按令牌改自己的在手任务，不再要求租约未过期。进程身份冲突和队列 `active_leases` 也把进行中过期槽算进去，避免第二个进程把还在画的任务当成没人认领。领取前先清过期闲置租约。原生 `/api/image-tasks` 与图库维护 503 文案与公开接口对齐。
+ [优化] 账号均匀调度：空闲号优先，再派总生图（成功+失败+在途）少的号；官网卡住超过 60 秒的号先避开，别的号都满了再派。公开生图走队列，按库里的账号租约排序；未托管同步对话用同一套排序函数，在途计数来自进程内 inflight。
+ [优化] 安装向导不再询问后端同步并发容量。
+ [优化] 注册账号不再提供「每代理最大并发」；并发只由线程数控制，旧配置里的该字段加载时丢弃。
+ [优化] 注册页菜单改为「注册账号」，工作区布局下可滚完邮箱来源；注册代理只接受自定义住宅代理地址，默认出口、直连和代理组不再用于注册。
+ [修复] 注册代理打码值不能写回存储；旧的默认/直连/代理组配置加载时就会清掉。没有住宅代理时注册会话不会改走默认出口；运行中任务显示已暂停。`socks://` 与后端一样按住宅代理地址收下。
+ [修复] 注册住宅代理只用于 ChatGPT 注册会话；接码直连，验活和额度刷新走全局代理，不写回账号代理。
+ [新增] 公开图片模型增加 `gpt-image-2.5`、`gpt-image-2.5-flare`、`gpt-image-2.5-sunburst` 别名；Web 上游对话 slug 落到 `auto`，默认仍是 `gpt-image-2`。三个名字走同一条网页生图线，不能当成官方 Flare/Sunburst 分引擎。
+ [修复] 配置目录合并 Web 图片 2.5 别名时忽略大小写；公开队列门禁不再和 helper 里另一套同名模型集合混用。
+ [优化] 一键安装改为分步向导：回车显示默认值，固定仓库 `main` 与本地 PostgreSQL 18，管理员密钥隐藏确认且不能空过，摘要确认后才开始安装。
+ [优化] Docker Compose 使用独立网 `gptimage2api-network`（本地为 `gptimage2api-local-network`），应用和 PostgreSQL 不再走默认桥接。
+ [优化] 安装脚本和标准 Compose 的默认 Web/API 端口改为 `2080`。
+ [优化] 安装向导增加图片访问地址，写入 `GPTIMAGE2API_BASE_URL`；默认 `http://localhost:<端口>`，须为 http/https 且路径为空或 `/images`。
+ [修复] Python 安装模式把 PostgreSQL 只绑到 `127.0.0.1`，启动前先打印就绪地址和打码密钥，历史改写后无法快进时提示删目录重装。
+ [修复] 安装向导横幅在选语言前使用中英对照；端口和并发在输入时校验，不再等到确认后才失败。
+ [修复] 图片队列 schema 回填不再把整数字段写成空字符串，避免 PostgreSQL 新装启动失败。
+ [修复] 图片队列 schema 校验把 PostgreSQL `BYTEA` 当成 `BLOB`，不再因此拒绝启动。

## 1.0.1 - 2026-09-11

+ [新增] 建立 `gptimage2api` 项目，整合账号池、注册账号、四种邮箱接码和图片工作流。
+ [新增] 注册账号仅保留 `yyds_mail`、`remail`、`outlook_token`、`icloud_api` 四种邮箱供应商，并将成功注册结果写入上游账号仓库。
+ [新增] 发布 GitHub Release 在线更新包，受管 Docker 控制台可检查并一键安装新版本。
+ [优化] 图片生成统一进入独立 PostgreSQL 持久化队列，在单个 App 进程内使用有界 Worker 执行、重试和恢复，不再依赖 Cluster 或独立 `image-worker` 容器。
+ [优化] 生图请求缺少幂等头时由服务端生成请求标识，同时继续保留显式幂等键和任务去重语义。
+ [修复] 注册核心结果增加持久化待处理收口、幂等入库、账号刷新、指数退避重试和管理员重试接口，避免注册成功后因后处理失败而丢失账号。
+ [修复] PostgreSQL Compose 固定 `gptimage2api_app` 与 `gptimage2api_image_queue` 数据库名，并在健康检查和升级路径中幂等补建队列数据库。
+ [修复] Image Queue Store 使用有序版本迁移并拒绝未来版本；安装器生成 dotenv 时对特殊字符进行安全转义。
+ [修复] Codex 生图统一复用配置的代理 Session、图片出口和真实 Target Route；网络、超时、Arkose 能力缺口和额度别名保留结构化错误语义。
+ [修复] 注册运行时在长时间接码期间持续续租，停止时取消排队 Future；Outlook 状态使用跨进程文件锁并拒绝未知重置范围和运行中重置。
+ [修复] 注册邮箱最终状态在账号验活和入库前完成；邮箱回写失败不再计为注册成功，核心结果继续保留待收口。
+ [修复] Application Database 中的注册配置改为 AES-GCM 加密存储，备份恢复前停止注册账号与图片队列并要求重启。
+ [修复] 上游 Chrome146 指纹收口为同一套 Client Hints、时区、视口和 PoW 环境；匿名对话不再混用洛杉矶时区。
+ [修复] 持久化生图队列在额度耗尽、限流和鉴权失败时换号重试，不再把所有 429/502 都当成原账号瞬时故障。
+ [修复] FlareSolverr 清关模式在配置中归一成 none；Chrome146 会话只接受手动 cf_clearance。
+ [修复] 设置契约不再把 flaresolverr 列为可用清关模式，并移除未使用的 FlareSolverr 客户端和 flaresolverr_url 字段。
+ [修复] ChatGPT 403/5xx（upstream_unavailable）会保留原 conversation 并在同一账号上恢复；重试新会话会重新挂上手动 cf_clearance。
+ [修复] 未分类的上游 4xx（upstream_error）改为同账号有限重试，不再清 conversation 换号。
+ [修复] 代理运行时配置不再写入残留的 flaresolverr_url 字段。
+ [修复] 注册静态请求头改为走 chrome146_headers，PoW 配置不再接收无效的 User-Agent 参数。
+ [修复] OAuth 静态请求头不再重复手写 Client Hints，改由 chrome146_headers 强制收口。
+ [修复] Chrome146 User-Agent 收到 contracts/chrome146.json，Python 和前端共用同一份。
+ [修复] Docker 控制台构建复制 `contracts/chrome146.json`，避免镜像内前端指纹档案缺失。
+ [修复] 图片下载失败按 download_attempts 同账号重试，并保留 conversation。
+ [修复] 协议等待超时（任务仍在跑）记为 pending，不再记成调用失败。
+ [修复] insufficient_quota 与额度耗尽一样会换号重试。
+ [修复] PPT/PSD 任务会持久化请求内容；重启后未完成任务会重新入队，错误态同一 client_task_id 可重跑。
+ [修复] 上游挑战（upstream_challenge_required）会按生成预算同账号重试。
+ [修复] 图片 SSE/轮询超时会保留原 conversation 并在同一账号上恢复，不再当成换号失败。
+ [修复] 上游连接失败/超时按同一账号恢复处理，不再清掉可继续轮询的 conversation。
+ [修复] 生图队列在 conversation 已检查点后仍对额度/限流/鉴权失败换号，并清掉旧会话；空结果也会换号重试。
+ [修复] PoW 日期使用 Chrome Date.toString() 空格填充日，且 hardwareConcurrency 与 Chrome146 档案一致。

## 3.2.2 - 2026-08-11

+ [新增] 设置页新增控制台请求超时、图片首次轮询等待和图片轮询间隔，默认分别为 600 秒、5 秒和 5 秒。
+ [优化] 日志、图片、账号和设置等控制台请求共用可配置的等待上限，登录状态检查保留 8 秒；联网搜索固定使用 10 分钟总时限并额外预留 30 秒返回最终响应，可编辑文件任务固定使用 20 分钟总时限。
+ [优化] 管理员登录或刷新控制台时检查一次新版本；发现更新后 Header 与版本弹窗统一以黄色提示，打开版本弹窗不再重复检查。
+ [优化] 对话画图的参考来源在宽屏中停靠并挤压对话区，窄屏复用统一抽屉；来源列表独立滚动并保留引用高亮。
+ [优化] 概览中心统一使用 12px 卡片圆角，并将模型请求分布的统计口径说明贴近标题展示。
+ [修复] 概览中心调用活跃度在当前统计周期没有请求时展示空态提示。

## 3.2.1 - 2026-08-10

+ [优化] 模型请求分布补充统计口径说明，仅统计成功及部分成功请求。
+ [修复] Dashboard 只在小时聚合表结构不兼容时重建完整投影；仅状态表变化时保留兼容的 30 天历史并重新建立 Call Record 检查点。
+ [修复] 用户删除调用日志后不再触发 Dashboard 全量重建，避免较短的日志保留周期使 7 天和 30 天统计随之收缩。

## 3.2.0 - 2026-08-10

+ [新增] 概览中心新增运行环境面板，展示运行方式、应用版本、实例与系统信息、CPU 与内存占用、数据盘、服务运行时间及网络速率。
+ [新增] 设置页新增概览中心偏好，可设置默认统计周期和 5–300 秒自动刷新间隔；默认使用 24 小时和 10 秒，并保存在当前浏览器。
+ [优化] 概览中心重新组织账号、调用与运行环境信息，保留模型请求分布，并提供调用结果、调用活跃度、成功耗时和模型耗时视图，各图表可独立切换 24 小时、7 天和 30 天。
+ [优化] 概览数据统一由后端生成单次快照；进入页面立即请求一次，页面可见时按设置间隔串行刷新，页面隐藏后停止，后台刷新保留最近一次可用数据。
+ [优化] 在线更新在执行前显示目标版本、预计耗时和自动重启提示，确认后才开始安装。
+ [优化] 统一代码许可证、中英文文档标识和项目元数据为 AGPL-3.0，并保留上游 MIT 版权与许可声明。
+ [优化] Python 依赖恢复使用官方 PyPI，移除项目内固定的第三方镜像源。
+ [修复] 修改概览中心默认周期后，返回已缓存的概览页面会立即应用新设置，不影响页面内各图表的独立切换。
+ [修复] 调用活跃度恢复格子悬浮详情，并在首次进入和切换统计周期时播放进入动画。

## 3.1.1 - 2026-08-08

+ [优化] 概览指标改用增量持久化投影，成功率、成功耗时、模型成功请求分布和当前并发使用统一的数据契约。
+ [修复] GenBox 推送仅接受已登记在 Gallery 索引中的有效图片，推送时携带生成元数据并保留源图片；未登记、外部来源或无效内容不会进入推送队列。
+ [修复] Studio 自动推送在 `GPTIMAGE2API_BASE_URL` 为空、按当前请求生成图片地址时仍可正常识别来源，同时继续执行来源校验与 Gallery 读取门控。
+ [修复] 更新中心稳定读取 GitHub 的远程 CHANGELOG；远程日志不可用时回退到 Release 内容，版本条目可正常解析展示。
+ [修复] 可滚动弹层在桌面和窄屏视口保持统一高度，最小化的任务与请求详情面板支持拖拽定位。
+ [修复] 发布验证流程不再引用被仓库忽略的本地测试源码，tag 发布可从干净检出完整执行。

## 3.1.0 - 2026-08-07

+ [新增] 管理控制台支持检查 GitHub Release，并在受管 Docker 运行时中执行持久化在线更新；后端统一拥有下载、校验、安装、回滚与重启任务，页面刷新后可继续查看进度。
+ [新增] 手动添加与编辑账号支持填写 AT 和 RT；编辑凭据时使用稳定管理 ID，留空不会覆盖已有凭据。
+ [新增] 账号导入支持选择目标账号组；CPA 与 Sub2API 远程导入全局只运行一个任务，Sub2API 测试连接后直接展示远程分组。
+ [新增] 账号管理支持导入本地 Sub2API JSON 文件，兼容常见导出结构与嵌套凭据字段。
+ [新增] 账号批量处理支持跨分页全选当前筛选结果。
+ [新增] 图片管理支持按配置手动推送到 GenBox，Studio 可在生成成功后通过有界队列自动推送。
+ [优化] 设置页的远程 CPA 与 Sub2API 导入统一使用账号批量操作抽屉展示进度，并支持离开页面后恢复进行中的任务。
+ [优化] 图片与调用日志自动清理统一使用小时保留周期，默认保留 24 小时；日志按精确时间滚动清理。

## 3.0.0 - 2026-08-05

+ [新增] 账号、用户密钥、设置、调用日志和指标统一使用 Application Database，支持 SQLite 与 PostgreSQL 18。
+ [新增] 一键安装支持 SQLite、本地 PostgreSQL 18 容器和已有 PostgreSQL URL，并提供可叠加的 PostgreSQL Compose 配置。
+ [新增] 提供 Chat Completions、Responses、Messages、搜索、图片生成、图片编辑和 PPT / PSD 可编辑文件任务接口。
+ [新增] Vue 3 管理控制台覆盖概览、账号、代理、日志、实时监控、图片管理、对话画图和系统设置。
+ [新增] 账号管理支持 OAuth、Access Token、Session JSON、CPA、远程 CPA 和 Sub2API 导入，以及指定账号的文本与图片测试。
+ [新增] 对话画图支持文本、搜索、文生图、图生图、多图参考、局部编辑、Markdown、任务恢复和本地提示词库。
+ [新增] 提示词库支持从 [yukkcat/image-prompts](https://github.com/yukkcat/image-prompts) 更新本地快照，Studio 按本地快照检索并在滚动时增量加载。
+ [新增] 图片管理支持本地与 WebDAV 存储、标签、下载、压缩、清理和存储状态查看。
+ [新增] 代理管理支持账号代理、账号组代理、多出口代理组、节点图片并发、默认出口和备用出口。
+ [优化] 账号状态、代理关系、失败分类、日志详情和监控详情统一由后端输出业务投影，前端负责交互与展示。
+ [优化] 批量刷新、同步、导入和账号核验共用账号处理并发设置，批量进度按账号或资源实时展示。
+ [优化] 图片请求统一记录账号、出口、上游错误和各阶段耗时，实时监控与持久化日志复用同一条诊断链路。
+ [优化] 桌面端与移动端统一表格、列表、分页、加载、空状态、菜单、抽屉和滚动规则，列表支持固定视口与随页面展开。
+ [优化] 概览指标采用缓冲写入并保留最近 30 天；调用卡片直接使用汇总与切号统计，调用结果、成功率、成功耗时和切号明细统一按时间展示。
+ [优化] 后端同步工作线程默认并发容量调整为 120，不设置人为最高值。
+ [优化] R2 备份覆盖 Application Database，并可按设置包含图片、图片任务与生成文件。
+ [优化] Application Database 直接按 3.0 当前模型初始化，不保留旧版 schema 版本表或数据迁移分支。
+ [优化] 设置页将无限画布归入“外部服务”，统一用户密钥、CPA 与 Sub2API 的页面容器、圆角和响应式宽度。
+ [优化] 删除已停用注册账号遗留的 WARP、Privoxy 与 FlareSolverr Compose 部署入口，安装脚本只保留当前支持的运行方式。
+ [修复] AT 失效时使用有效 RT 刷新；AT 失效且没有可用 RT 的账号不会继续显示为正常或参与调度。
+ [修复] 图片失败统一决定公开错误、重试、账号处置和诊断信息，减少不同接口之间的行为漂移。
+ [修复] Studio 可恢复的图片任务继续轮询原任务，避免重复创建任务或裁剪对话历史。
+ [修复] 文件任务按 API Key 隔离创建、查询和删除，成功产物可通过随机公开链接下载。
+ [修复] 提示词更新请求允许较慢的远端下载，提示词弹窗滚动到底部后可继续加载后续内容。
+ [修复] 一键安装脚本支持通过 `curl | bash` 从标准输入执行，不再因缺少 `BASH_SOURCE[0]` 提前退出。
